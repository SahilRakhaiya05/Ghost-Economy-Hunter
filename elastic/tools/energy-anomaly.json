{
  "id": "ghost-energy-anomaly",
  "name": "energy_anomaly",
  "type": "esql",
  "description": "Finds buildings with high energy consumption and very low occupancy, indicating energy-occupancy divergence waste. Flags buildings below 15% occupancy consuming over 5000 kWh.",
  "tags": ["analytics", "real-estate", "waste-detection"],
  "query": "FROM nyc-buildings | STATS avg_occupancy = AVG(occupancy_pct), total_energy_kwh = SUM(energy_kwh) BY building_id, borough | EVAL total_energy_cost = total_energy_kwh * 0.22, waste_score = (1 - avg_occupancy) * total_energy_kwh | WHERE avg_occupancy < 0.15 AND total_energy_kwh > 5000 | SORT total_energy_cost DESC | LIMIT 15"
}
