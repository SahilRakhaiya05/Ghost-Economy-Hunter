{
  "name": "value_calculator",
  "description": "Looks up unit cost for a given item key from the pricing reference index",
  "type": "esql",
  "parameters": [
    {
      "name": "item_key",
      "type": "string",
      "description": "The item identifier to look up (e.g. insulin, press-machine-hour, kwh-nyc)"
    }
  ],
  "query": "FROM pricing-reference\n| WHERE item_key == ?item_key\n| KEEP item_key, item_name, unit_cost_usd, unit_label\n| LIMIT 1"
}
